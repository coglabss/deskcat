{
  "name": "desktop-cat",
  "version": "0.1.0",
  "description": "A cute free-roaming desktop pet cat",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "test": "jest",
    "dist": "electron-packager . DesktopCat --overwrite --out=dist --app-version=0.1.0"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "electron": "^31.0.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "electron-store": "^8.2.0"
  }
}
